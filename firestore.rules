rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allows read/write for authenticated admin only.
      // For production, replace with proper auth rules.
      allow read, write: if true;
    }
  }
}
